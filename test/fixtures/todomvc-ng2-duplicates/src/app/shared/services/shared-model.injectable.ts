import { Injectable } from '@angular/core';

/**
 * An injectable with the same name as a plain class, used to test cross-category duplicate name handling.
 */
@Injectable()
export class SharedModel {}
