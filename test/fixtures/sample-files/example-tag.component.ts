import { Component } from '@angular/core';

/**
 * A component used to verify that class-level @example JSDoc tags are rendered.
 *
 * @example
 * <app-example-tag></app-example-tag>
 */
@Component({
    selector: 'app-example-tag',
    template: `<div></div>`
})
export class ExampleTagComponent {}
