import { Route } from '@angular/router';

import { StandaloneHomeComponent } from './standalone-home.component';

export const routes: Route[] = [
    {
        path: '',
        component: StandaloneHomeComponent,
    },
];
