<ol class="breadcrumb">
  <li><a href="/components.html">Components</a></li>
  <li>{{name}}</li>
</ol>

<p>
    <b>File: </b>{{component.file}}
</p>
<p>
    <b>Selector: </b>{{component.selector}}
</p>

<div class="container-fluid module">
    <div class="row">
        {{#if component.declarations}}
        <div class="col-sm-3">
            <h4>Declarations</h4>
            <ul class="list-group">
                {{#each component.declarations}}
                    <li class="list-group-item">
                        <a {{#compare type "===" 'component'}} href="../components/{{name}}.html" {{/compare}}>{{name}}</a>
                    </li>
                {{/each}}
            </ul>
        </div>
        {{/if}}
        {{#if component.providers}}
        <div class="col-sm-3">
            <h4>Providers</h4>
            <ul class="list-group">
                {{#each component.providers}}
                    <li class="list-group-item">
                        {{name}}
                    </li>
                {{/each}}
            </ul>
        </div>
        {{/if}}
    </div>
</div>
