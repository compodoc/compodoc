<ol class="breadcrumb">
  <li>Injectables</li>
  <li>{{name}}</li>
</ol>

{{#unless disableSourceCode}}
<ul class="nav nav-tabs" role="tablist">
    <li class="active">
        <a href="#infos" id="infos-tab" role="tab" data-toggle="tab">Infos</a>
    </li>
    <li>
        <a href="#source" role="tab" id="source-tab" data-toggle="tab">Source</a>
    </li>
</ul>

<div class="tab-content">
    <div class="tab-pane fade active in" id="infos">
        {{/unless}}
        <p class="comment">
            <h3>File</h3>
        </p>
        <p class="comment">
            <code>{{injectable.file}}</code>
        </p>

        {{#if injectable.description}}
            <p class="comment">
                <h3>Description</h3>
            </p>
            <p class="comment">
                {{{parseDescription injectable.description}}}
            </p>
        {{/if}}

        {{#if injectable.constructorObj}}
            {{> block-constructor constructor=injectable.constructorObj }}
        {{/if}}

        {{#if injectable.methods}}
            {{> block-method methods=injectable.methods }}
        {{/if}}

        {{#if injectable.properties}}
            {{> block-property properties=injectable.properties }}
        {{/if}}
    {{#unless disableSourceCode}}
    </div>
    <div class="tab-pane fade tab-source-code" id="source">
        <pre><code class="hljs javascript">{{injectable.sourceCode}}</code></pre>
    </div>
</div>
{{/unless}}
