<ol class="breadcrumb">
  <li>Classes</li>
  <li>{{name}}</li>
</ol>

{{#unless disableSourceCode}}
<ul class="nav nav-tabs" role="tablist">
    <li class="active">
        <a href="#infos" id="infos-tab" role="tab" data-toggle="tab">Infos</a>
    </li>
    <li>
        <a href="#source" role="tab" id="source-tab" data-toggle="tab">Source</a>
    </li>
</ul>

<div class="tab-content">
    <div class="tab-pane fade active in" id="infos">
        {{/unless}}
        <p class="comment">
            <h3>File</h3>
        </p>
        <p class="comment">
            <code>{{class.file}}</code>
        </p>

        {{#if class.description}}
            <p class="comment">
                <h3>Description</h3>
            </p>
            <p class="comment">
                {{{parseDescription class.description}}}
            </p>
        {{/if}}

        {{#if class.constructorObj}}
            {{> block-constructor constructor=class.constructorObj }}
        {{/if}}

        {{#if class.methods}}
            {{> block-method methods=class.methods }}
        {{/if}}

        {{#if class.indexSignatures}}
            {{> block-index indexables=class.indexSignatures }}
        {{/if}}

        {{#if class.properties}}
            {{> block-property properties=class.properties }}
        {{/if}}
    {{#unless disableSourceCode}}
    </div>
    <div class="tab-pane fade tab-source-code" id="source">
        <pre><code class="hljs javascript">{{ class.sourceCode}}</code></pre>
    </div>
</div>
{{/unless}}
